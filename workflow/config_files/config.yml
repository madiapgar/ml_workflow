## yay!! the config file!!

project_name: "outputs/deltas_diet_ml"
metadata: "data/ml_approved_metadata.tsv"
otu_table: "data/family_deltas_wide.tsv"
meta_dictionary: "data/meta_dict_keys.tsv"
sample_id_col: "mouse_id"
y_predict_col: "diet"
boruta_shap: True
## if you have "ml" set up you can use the conda env
## you can also use the "ml_env.yml" in workflow/envs, the first option is just faster
conda_env: "ml"
